services:
  app:
    build: .
    container_name: fastapi_app
    ports:
      - "8010:8000"
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      MONGO_URL: ${MONGO_URL}
    depends_on:
      - mongodb

  mongodb:
    image: mongo
    container_name: mongodb
